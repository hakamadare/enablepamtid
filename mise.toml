[tools]
go = "latest"
goreleaser = "latest"

[tasks.build]
description = "Build the CLI (pass `--snapshot` to build HEAD)"
run = "goreleaser build --clean"

[hooks]
enter = "mise install"

[settings]
experimental = true
